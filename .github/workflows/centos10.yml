name: CentOS Stream
on: [push]

jobs:
  Centos10:
    runs-on: ubuntu-24.04
    container: quay.io/centos/centos:stream10
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/deploy-centos-10
