name: AlmaLinux
on: [push]

jobs:
  Almalinux9:
    runs-on: ubuntu-24.04
    container: almalinux:9
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/deploy-almalinux-9
  Almalinux10:
    runs-on: ubuntu-24.04
    container: almalinux:10
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/deploy-almalinux-10
